//$(document).bind('deviceready', function(){
function onDeviceReady() {
    //$(function(){
       alert('MyRobot app started with moved js dir');
       $('#logon_button').click(function(){ alert('logon button clicked with js dir moved'); });
    //});
//});
}
